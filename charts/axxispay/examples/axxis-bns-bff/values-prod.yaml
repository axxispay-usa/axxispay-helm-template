# Deploy: helm upgrade --install axxis-bns-bff axxispay/helm-template -f values-prod.yaml --set image.tag=<GIT_SHA>
# Todos os recursos serão criados com o nome: axxis-bns-bff
namespace: bns

image:
  repository: 000000000000.dkr.ecr.us-east-1.amazonaws.com/axxis-bns-bff
  tag: latest

ingress:
  hosts:
    - host: bff.example.com

hpa:
  minReplicas: 2
  maxReplicas: 10

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    memory: 512Mi

externalSecret:
  refreshInterval: 1h
  dataFrom:
    - extract:
        key: namespace/app-name

configmap:
  data:
    BFF_PORT: "8080"
    BACKEND_ADIANTAMENTO_URL: "http://ads-service/api/v1"
    BACKEND_BNS_URL: "http://backend-service/api/v1"
    BNS_API_BASE_URL: "http://backend-service/api/v1"
    BNS_OAUTH_CLIENT_ID: "adiantamento-salario"
