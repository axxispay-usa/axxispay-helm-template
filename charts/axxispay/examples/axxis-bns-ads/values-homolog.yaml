# Deploy: helm upgrade --install axxis-bns-ads axxispay/helm-template -f values-homolog.yaml
# Todos os recursos serão criados com o nome: axxis-bns-ads
namespace: bns

image:
  repository: 000000000000.dkr.ecr.us-east-1.amazonaws.com/axxis-bns-ads
  tag: homolog

ingress:
  hosts:
    - host: ads.homolog.example.com

hpa:
  minReplicas: 1
  maxReplicas: 3

externalSecret:
  refreshInterval: 5m
  dataFrom:
    - extract:
        key: namespace/app-name

configmap:
  data:
    APP_PORT: "8080"
    BACKEND_BNS_URL: "http://backend-service/api/v1"
